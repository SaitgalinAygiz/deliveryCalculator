let getters = {
    results: state => {
        return state.results
    },

    coord: state => {
        return state.coord
    },
    trackingResults: state => {
        return state.trackingResults
    }
};

export default getters;
